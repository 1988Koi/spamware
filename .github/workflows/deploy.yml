name: Build and Deploy Docker Images

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Faz checkout do código
      - uses: actions/checkout@v3

      # Configura Docker
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Backend: build Docker
      - name: Build backend Docker image
        run: docker build -t spamton-backend ./backend

      # Frontend: build Docker
      - name: Build frontend Docker image
        run: docker build -t spamton-frontend ./frontend

      # (Opcional) Push para Docker Hub ou Azure Container Registry
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      #
      # - name: Push backend image
      #   run: docker push yourusername/spamton-backend:latest
      #
      # - name: Push frontend image
      #   run: docker push yourusername/spamton-frontend:latest
